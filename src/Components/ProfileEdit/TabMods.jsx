import React, { Component } from 'react'

import ProfileProjects from '../Profile/ProfileProjects';

export default class TabMods extends Component {
  render() {
    return (
      <section className="tab">
        <ProfileProjects />
      </section>
    )
  }
}
